
import unittest

class HomePageTest(unittest.TestCase):

    def SetUp(self):
        if os.name=='nt'
            self.browser = webdriver.Chrome()

        else:
            self.browser = webdriver.Firefox()
